# 2026-06-01

## Phase 4: Asset Pipeline — 完成 ✅

### .env 配置
- Image: Tongyi (DashScope) ✅ — API 路由正常（403 是账户权限问题）
- Audio: OpenAI-compat (DeepSeek) ✅ — TTS endpoint 待验证
- Video: 未配置 (stub) 🚧
- LLM: DeepSeek v4 ✅

### 完成
- [x] AssetService 路由（image/audio/video 三模态路由）
- [x] AssetImageService（Tongyi + async task polling、OpenAI-compat、Doubao/Fal stub）
- [x] AssetAudioService（OpenAI TTS provider、Tongyi/Doubao stub）
- [x] AssetVideoService（全部 stub — 视频生成 API 尚不稳定）
- [x] AutoTiler（PIL-based tileset 切割）
- [x] TilesetProcessor（Phaser 兼容 JSON metadata）
- [x] GameSkill Phase 3 从 placeholder stub 升级为 AssetService 调用
- [x] .env 加载（python-dotenv + ConfigLoader.load(load_dotenv=True)）
- [x] CLI 命令全部启用 .env 加载
- [x] pyproject.toml 添加 Pillow 和 python-dotenv 依赖
- [x] 203 测试全部通过

### 遇到的问题
- **.env 污染测试**：`python-dotenv` 加载后写入 `os.environ`，影响 ConfigLoader 测试。修复：测试文件添加 `autouse=True` fixture 清理 .env 变量，ConfigLoader.load() 默认 `load_dotenv=False`
- **Tongyi API 403**：DashScope 账户可能需要开通图片合成服务权限（非代码问题）

### 决策记录
- AssetService 作为独立 router，与 GameSkill 解耦（通过 `set_asset_service()` 注入）
- 图片生成失败不阻塞 pipeline（单个 asset 的异常被 try/except 捕获，fallback 到 placeholder）
- 视频全部 stub（env 中未配置，且 Sora/Wan/Seedance API 尚不稳定）
- AutoTiler 依赖 Pillow 已于 pyproject.toml 中声明

### 项目统计
- Phase 4 新增源文件：6 个（asset services + auto_tiler + tileset_processor）
- Phase 4 修改文件：7 个（GameSkill、CLI commands、ConfigLoader、pyproject.toml）
- 总测试：203 passed

### 明日计划
- [ ] Phase 5: Integration（端到端测试、完整 pipeline 验证）
- [ ] 或 Phase 6: Bench + Polish（评估体系）
